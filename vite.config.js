import { defineConfig } from 'vite'
import { svelte } from '@sveltejs/vite-plugin-svelte'

// https://vite.dev/config/
export default defineConfig({
  plugins: [svelte()],
  base: '/InteractiveMusicAppTest/', // GitHub Pages 저장소 이름으로 설정
})
